import { HighlingtDirective } from './highlingt.directive';

describe('HighlingtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlingtDirective();
    expect(directive).toBeTruthy();
  });
});
